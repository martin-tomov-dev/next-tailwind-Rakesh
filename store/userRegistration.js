import { atom } from "jotai";

// User Registration flow
export const userRegistrationAtom = atom({});
export const userTitlesAtom = atom([]);
export const userSkillsAtom = atom([]);
export const userCertAtom = atom([]);
